import 'package:flutter/material.dart';

class PredictorTab extends StatelessWidget {
  const PredictorTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Predictor Content'),
    );
  }
}